/* Use the += operator to add number to a
variable and log the result to the console */

let initialValue = 10;
let numberToAdd = 5;

initialValue += numberToAdd;

console.log("The new value of the variable is:", initialValue);
