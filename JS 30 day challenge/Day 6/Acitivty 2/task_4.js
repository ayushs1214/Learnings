/* Use the 'pop' method to remove the last element from the array & log the updated array. */

const array = [1, 2, 3, 4, 5];

// Use the 'pop' method to remove the last element
array.pop();

// Log the updated array
console.log(array);
