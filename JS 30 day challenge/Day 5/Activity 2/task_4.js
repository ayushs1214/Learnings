/* Write a function expression to concatenate two strings & return the result */

const concatenateStrings = function(str1, str2) {
  return str1 + str2;
};

// Example usage
let result = concatenateStrings("Hello, ", "World!");
console.log(result); // Output: Hello, World!